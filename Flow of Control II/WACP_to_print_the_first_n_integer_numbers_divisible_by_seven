#include <stdio.h>

int main() {
    int n, count = 0;


    scanf("%d", &n);

    for (int i = 1; ; ++i) {
        if (i % 7 == 0) {
            printf("%d ", i);
            count++;
        }
        
        if (count == n) 
            break;
    }
    return 0;
}

